<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mz.system.provider.dao.SysMenuDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.mz.system.model.entity.SysMenuEntity" id="sysMenuMap">
        <id property="menuId" column="menu_id"/>
        <result property="menuName" column="menu_name"/>
        <result property="parentId" column="parent_id"/>
        <result property="orderNum" column="order_num"/>
        <result property="href" column="href"/>
        <result property="path" column="path"/>
        <result property="component" column="component"/>
        <result property="query" column="QUERY"/>
        <result property="isFrame" column="is_frame"/>
        <result property="isCache" column="is_cache"/>
        <result property="menuType" column="menu_type"/>
        <result property="visible" column="visible"/>
        <result property="status" column="STATUS"/>
        <result property="perms" column="perms"/>
        <result property="icon" column="icon"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>
    <!-- 获取数据库非空的字段
    SELECT sm.* FROM sys_role_menu srm
    RIGHT JOIN sys_menu sm ON sm.menu_id = srm.menu_id
    WHERE srm.role_id=2 and sm.perms  is not null and ltrim(sm.perms)<> ' '
    -->
    <!--<select id = "listByUserId" resultType = "com.xiaozheng.admin.security.to.MenuTreeDto">
        SELECT sm.*
        FROM sys_menu sm
                 LEFT JOIN sys_role_menu srm ON srm.menu_id = sm.menu_id
        WHERE srm.role_id in (SELECT sur.role_id FROM sys_user_role sur WHERE sur.user_id = #{userId})
    </select>-->


</mapper>